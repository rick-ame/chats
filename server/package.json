{
  "name": "server",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "clean": "rimraf dist",
    "dev": "tsx watch index.ts",
    "build": "tsc -b && tsc-alias",
    "start": "node dist/server/index.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "tsc-alias": "^1.8.15",
    "tsx": "^4.19.3"
  }
}
